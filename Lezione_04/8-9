list = ("Inter","Lazio","Milan","Roma")

def show_messages():
    i = 0
    for x in list:
        
        if i < len(list):

            print(list[0 + i])

            i +=1
        
        else:
           
            break
    
show_messages()