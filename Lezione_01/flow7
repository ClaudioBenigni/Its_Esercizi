pari = 0
dispari = 0
cont = 0

for i in range(10):
    
    n = int(input("Inserire un numero: "))

    if n%2==0:

        pari += 1
        
    else:

        dispari += 1

print(f"I numeri pari sono: {pari}")
print(f"I numeri dispari sono: {dispari}")